<Window x:Name="mWnd"
        x:Class="AMSRSE.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AMSRSE.Editor"
        xmlns:pkprfl="clr-namespace:AMSRSE.Editor.Controls.PickProfile"
        xmlns:sb="clr-namespace:AMSRSE.Editor.Controls.SpriteButton"
        xmlns:nvbtn="clr-namespace:AMSRSE.Editor.Controls.NavButton"
        xmlns:dv="clr-namespace:AMSRSE.Editor.Views.Dynamic"
        xmlns:ppdv="clr-namespace:AMSRSE.Editor.Views.Dynamic.PickProfile"
        SizeToContent="WidthAndHeight"
        mc:Ignorable="d"
        Title="MainWindow">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Width" Value="160" />
            <Setter Property="Height" Value="48" />
            <Setter Property="Background" Value="#00000000" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="BorderThickness" Value="4" />
            <Setter Property="BorderBrush" Value="#FF00A0FF" />
            <Setter Property="FontFamily" Value="{StaticResource Fonts.MetroidSR-Pixel}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#FF005180"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#FF00A0FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" TargetName="border" Value="#3FFFFFFF" />
                                <Setter Property="BorderBrush" TargetName="border" Value="#FFC0C0C0" />
                                <Setter Property="TextBlock.Foreground" TargetName="contentPresenter" Value="#FFC0C0C0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--<Style x:Key="MSRButton" TargetType="{x:Type sb:SpriteButton}" BasedOn="{StaticResource {x:Type sb:SpriteButton}}">
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="FontFamily" Value="{StaticResource Fonts.MetroidSR-Pixel}" />
            <Setter Property="FontSize" Value="16" />
        </Style>-->

        <!--<Style x:Key="YesNoDVStyle" TargetType="{x:Type dv:YesNoDV}" BasedOn="{StaticResource {x:Type dv:YesNoDV}}">
            <Setter Property="Width" Value="280" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="FontFamily" Value="{StaticResource Fonts.MetroidSR-Pixel}" />
            <Setter Property="FontSize" Value="16" />
        </Style>-->

        <!--<Style x:Key="MessageDVStyle" TargetType="{x:Type dv:MessageDV}" BasedOn="{StaticResource {x:Type dv:MessageDV}}">
            <Setter Property="Width" Value="280" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
            <Setter Property="FontFamily" Value="{StaticResource Fonts.MetroidSR-Pixel}" />
            <Setter Property="FontSize" Value="16" />
        </Style>-->


    </Window.Resources>

    <Grid Width="640" Background="#FF000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <pkprfl:ProfilesControl />
        <!--<dv:DynamicViewHost CurrentDynamicViewName="pkprflView">
            <dv:MultiButtonDV x:Name="pkprflView">
                <dv:MultiButtonDV.Buttons>
                    <nvbtn:NavButton
                        Style="{StaticResource MSRButton}"
                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                        Text="SAMUS A"
                        dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                        dv:DynamicViewHost.NavigationDirection="Forward" />
                    <nvbtn:NavButton
                        Style="{StaticResource MSRButton}"
                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                        Text="SAMUS B"
                        dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                        dv:DynamicViewHost.NavigationDirection="Forward" />
                    <nvbtn:NavButton
                        Style="{StaticResource MSRButton}"
                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                        Text="SAMUS C"
                        dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                        dv:DynamicViewHost.NavigationDirection="Forward" />
                </dv:MultiButtonDV.Buttons>

                <dv:MultiButtonDV x:Name="pkprflDecisionView">
                    <dv:MultiButtonDV.Buttons>
                        <nvbtn:NavButton
                            Style="{StaticResource MSRButton}"
                            SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                            UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                            Text="Edit Profile" />
                        <nvbtn:NavButton
                            Style="{StaticResource MSRButton}"
                            SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                            UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                            Text="New Game"
                            dv:DynamicViewHost.NavigateTo="newGameWarningView"
                            dv:DynamicViewHost.NavigationDirection="Forward"/>
                        <nvbtn:NavButton
                            Style="{StaticResource MSRButton}"
                            SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                            UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                            Text="Options"
                            dv:DynamicViewHost.NavigateTo="optionsView"
                            dv:DynamicViewHost.NavigationDirection="Forward" />
                        <nvbtn:NavButton
                            Style="{StaticResource MSRButton}"
                            SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                            UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                            Text="Back"
                            dv:DynamicViewHost.NavigateTo="pkprflView"
                            dv:DynamicViewHost.NavigationDirection="Backward" />
                    </dv:MultiButtonDV.Buttons>

                    <dv:MessageDV
                        x:Name="newGameWarningView" Style="{StaticResource MessageDVStyle}"
                        Message="Starting a new game will cause you to lose all of your current progress. All unlocked features and Options settings will remain.">
                        <dv:MessageDV.OKButton>
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="OK"
                                dv:DynamicViewHost.NavigateTo="newGameYNView"
                                dv:DynamicViewHost.NavigationDirection="Forward" />
                        </dv:MessageDV.OKButton>
                    </dv:MessageDV>
                    
                    <dv:YesNoDV x:Name="newGameYNView" Style="{StaticResource YesNoDVStyle}" Message="Start a new game?">
                        <dv:YesNoDV.YesButton>
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="Yes"
                                dv:DynamicViewHost.NavigateTo="gameModeSelectView"
                                dv:DynamicViewHost.NavigationDirection="Forward" />
                        </dv:YesNoDV.YesButton>
                        <dv:YesNoDV.NoButton>
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="No"
                                dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                                dv:DynamicViewHost.NavigationDirection="Backward" />
                        </dv:YesNoDV.NoButton>

                        <dv:MultiButtonDV x:Name="gameModeSelectView">
                            <dv:MultiButtonDV.Buttons>
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Normal"
                                    dv:DynamicViewHost.NavigateTo="confirmNewGameView"
                                    dv:DynamicViewHost.NavigationDirection="Forward" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Hard"
                                    dv:DynamicViewHost.NavigateTo="newHardGameWarningView"
                                    dv:DynamicViewHost.NavigationDirection="Forward" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Fusion"
                                    dv:DynamicViewHost.NavigateTo="newFusionGameWarningView"
                                    dv:DynamicViewHost.NavigationDirection="Forward" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Back"
                                    dv:DynamicViewHost.NavigateTo="newGameYNView"
                                    dv:DynamicViewHost.NavigationDirection="Backward" />
                            </dv:MultiButtonDV.Buttons>

                            <dv:MessageDV x:Name="newHardGameWarningView" Style="{StaticResource MessageDVStyle}"
                                          Message="In Hard mode, enemies are more powerful.">
                                <dv:MessageDV.OKButton>
                                    <nvbtn:NavButton
                                        Style="{StaticResource MSRButton}"
                                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                        Text="OK"
                                        dv:DynamicViewHost.NavigateTo="confirmNewGameView"
                                        dv:DynamicViewHost.NavigationDirection="Forward" />
                                </dv:MessageDV.OKButton>
                            </dv:MessageDV>

                            <dv:MessageDV x:Name="newFusionGameWarningView" Style="{StaticResource MessageDVStyle}"
                                          Message="In Fusion mode, enemies are even more powerful, and you can play as Fusion Suit Samus.">
                                <dv:MessageDV.OKButton>
                                    <nvbtn:NavButton
                                        Style="{StaticResource MSRButton}"
                                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                        Text="OK"
                                        dv:DynamicViewHost.NavigateTo="confirmNewGameView"
                                        dv:DynamicViewHost.NavigationDirection="Forward" />
                                </dv:MessageDV.OKButton>
                            </dv:MessageDV>

                            <dv:YesNoDV x:Name="confirmNewGameView" Style="{StaticResource YesNoDVStyle}"
                                        Message="Current data will be erased. Start a new game in #S.MODE# mode?">
                                <dv:YesNoDV.YesButton>
                                    <nvbtn:NavButton
                                        Style="{StaticResource MSRButton}"
                                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                        Text="Yes"
                                        dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                                        dv:DynamicViewHost.NavigationDirection="Backward" />
                                </dv:YesNoDV.YesButton>
                                <dv:YesNoDV.NoButton>
                                    <nvbtn:NavButton
                                        Style="{StaticResource MSRButton}"
                                        SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                        UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                        Text="No"
                                        dv:DynamicViewHost.NavigateTo="gameModeSelectView"
                                        dv:DynamicViewHost.NavigationDirection="Backward" />
                                </dv:YesNoDV.NoButton>
                            </dv:YesNoDV>
                        </dv:MultiButtonDV>
                    </dv:YesNoDV>
                    <dv:MultiButtonDV x:Name="optionsView">
                        <dv:MultiButtonDV.Buttons>
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="Controls" />
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="Gallery"
                                dv:DynamicViewHost.NavigateTo="galleryOptionsView"
                                dv:DynamicViewHost.NavigationDirection="Forward" />
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="amiibo" />
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="Staff Credits" />
                            <nvbtn:NavButton
                                Style="{StaticResource MSRButton}"
                                SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                Text="Back"
                                dv:DynamicViewHost.NavigateTo="pkprflDecisionView"
                                dv:DynamicViewHost.NavigationDirection="Backward" />
                        </dv:MultiButtonDV.Buttons>

                        <dv:MultiButtonDV x:Name="galleryOptionsView">
                            <dv:MultiButtonDV.Buttons>
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Unlock Chozo Memories" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Unlock Metroid II Art" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Unlock Concept Art" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Unlock Music" />
                                <nvbtn:NavButton
                                    Style="{StaticResource MSRButton}"
                                    SelectedSprite="{StaticResource Images.UI.MSRButton-Active}"
                                    UnselectedSprite="{StaticResource Images.UI.MSRButton-Normal}"
                                    Text="Back"
                                    dv:DynamicViewHost.NavigateTo="optionsView"
                                    dv:DynamicViewHost.NavigationDirection="Backward" />
                            </dv:MultiButtonDV.Buttons>
                        </dv:MultiButtonDV>
                    </dv:MultiButtonDV>
                </dv:MultiButtonDV>
            </dv:MultiButtonDV>
        </dv:DynamicViewHost>-->
        <!--<ItemsControl x:Name="slidingButtons" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="btnControls" Content="Controls" />
            <Button x:Name="btnGallery" Content="Gallery" />
            <Button x:Name="btnAmiibo" Content="amiibo" />
            <Button x:Name="btnStaffCredits" Content="Staff Credits" IsEnabled="False" />
        </ItemsControl>-->
        <!--<pkprfl:ProfilesControl Grid.Row="1" />-->
        <!--<ppdv:SelectedProfileActionsDV />-->
    </Grid>

    <!--<Window.Triggers>
        <EventTrigger SourceName="btnGallery" RoutedEvent="Button.Click">
            <BeginStoryboard>
                <Storyboard
                    SpeedRatio="4">
                    <ThicknessAnimation
                        Storyboard.TargetName="slidingButtons"
                        Storyboard.TargetProperty="Margin"
                        To="-64,0,0,0" />
                    <DoubleAnimation
                        Storyboard.TargetName="slidingButtons"
                        Storyboard.TargetProperty="Opacity"
                        To="0" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>-->
</Window>
