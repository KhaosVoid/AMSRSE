<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:cb="clr-namespace:MSRSaveEditor.Controls.ColorBlended">
    <!--<Style TargetType="{x:Type cb:ColorBlendedButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type cb:ColorBlendedButton}">
                    <Rectangle x:Name="testRect" Fill="#7FFFFFFF">
                        <Rectangle.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorLevel=1, AncestorType={x:Type cb:ColorBlendedButton}}, Path=IsSelected}" Value="True">
                                <Setter TargetName="testRect" Property="Fill" Value="#FF51B6E6" />
                            </DataTrigger>
                        </Rectangle.Triggers>
                    </Rectangle>
                    <ControlTemplate.Triggers>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>