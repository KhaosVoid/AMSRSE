<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:pkprfl="clr-namespace:AMSRSE.Editor.Controls.PickProfile">
    <Style TargetType="{x:Type pkprfl:ProfilesControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type pkprfl:ProfilesControl}">
                    <Grid Width="640">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <pkprfl:ProfileItem x:Name="PART_ProfileItem0" DataContext="{TemplateBinding Profile0Data}" />
                        <pkprfl:ProfileItem x:Name="PART_ProfileItem1" Grid.Row="1" DataContext="{TemplateBinding Profile1Data}" />
                        <pkprfl:ProfileItem x:Name="PART_ProfileItem2" Grid.Row="2" DataContext="{TemplateBinding Profile2Data}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>